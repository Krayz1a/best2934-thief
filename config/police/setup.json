{
  "_schema": "PRIVATE per-peer setup (book Appendix B). Never crosses the network, never signed, never negotiated. Contains no secrets -- API keys and OAuth credentials come from environment variables only (guidelines §7.4).",
  "version": "1.00",
  "game": {
    "group_id": "best2934",
    "group_name": "best2934",
    "members": [
      "Tomer Levy",
      "Eyal Koloshi",
      "Alon Issman"
    ],
    "repos": {
      "cop": "https://github.com/Krayz1a/best2934-cop",
      "thief": "https://github.com/Krayz1a/best2934-thief"
    }
  },
  "opponents": {
    "_remark": "Terms the book leaves to inter-team agreement that are genuinely PER-PAIR rather than league-wide. They live here, in the private unsigned setup, because game.json is hashed into config_sha256 -- adding a key there would move a digest both peers have already verified. Defaults are first_half + multiplicative_book_v1, so an opponent absent from this table is played on the book's own terms.",
    "gal-roy1": {
      "role_convention": "first_half",
      "scent_model": "multiplicative_book_v1",
      "_remark": "Pinned by them in CONNECT.md section 6 and played end to end twice.",
      "tie_rule": "series_add"
    },
    "imreeyal": {
      "role_convention": "odd_even",
      "scent_model": "subtractive_chebyshev_v1",
      "_remark": "The copthief-league-protocol kit's published forms, which anrbj666 and uoh-sqak also play. Under odd_even the first-sorted group is cop on sub-games 1/3/5, so best2934 is cop on 1/3/5 and imreeyal on 2/4/6.",
      "tie_rule": "series_add"
    },
    "_tie_rule_remark": "series_add | series_replace | per_subgame. Three teams can each be conformant and each compute a different total for the same level series, so this is declared rather than derived. series_add is the league's common form (this kit, imreeyal, anrbj666); per_subgame is what the unmodified reference actually does -- it pays a drawn ROW 2 apiece and applies nothing at series level, which agrees with series_add whenever some sub-game tied and differs whenever a series ties with no drawn row."
  },
  "network": {
    "_remark": "public_url is this role's path behind the shared front door (tools/frontdoor.py). Both roles are served at once from one reserved ngrok domain, so nothing is repointed between sub-games -- required by the odd/even role convention, where the role flips at every sub-game rather than once at half time.",
    "my_port": 8801,
    "public_url": "https://monogram-radio-blooper.ngrok-free.dev/cop/mcp",
    "opponent_url": "http://127.0.0.1:8802/mcp",
    "turn_timeout_seconds": 30
  },
  "strategy": {
    "_remark": "brain accepts 'package.module:Class' so a strategy can be swapped without touching the engine (guidelines §12.1, extension points).",
    "brain": "",
    "_barrier_engage_range_remark": "Tuned from 4 to 1 on measured evidence (results/sweep.json, results/robustness.json). A barrier costs a turn of movement, so engaging from a distance leaves the cop standing still while the thief walks away -- and under the agreed one-turn scent lag the belief peak it was aiming at is probably wrong too. Re-measured under that lag, the curve is a cliff rather than a slope: capture 1.000 at range 1, 0.950 at 2, then 0.000 at 3 and 0.025 at 4. Verified against five structurally different thieves, not only the one the sweep used -- range 1 has the best worst case (0.925) of every level tested. NOTE the class default in CopBrain is still 4; this file is what we field, and measuring the class defaults instead is exactly the fault recorded as ADR-025.",
    "barrier_engage_range": 1,
    "barrier_min_gain": 1,
    "barrier_endgame_reserve": 3
  },
  "trash_talk": {
    "_remark": "provider: template | ollama | claude_api | claude_cli. 'template' costs zero tokens and is the default, because the book requires the match to be playable with no LLM at all.",
    "provider": "template",
    "every_n_steps": 1,
    "seed": 0
  },
  "llm": {
    "model": "claude-haiku-4-5",
    "max_tokens": 128,
    "api_key_env": "ANTHROPIC_API_KEY",
    "ollama_host": "http://127.0.0.1:11434",
    "ollama_model": "llama3.2:1b"
  },
  "email": {
    "_remark": "recipient is overwritten at load time with the address fixed by Appendix F Table 20 -- a team cannot opt out of reporting by redirecting it.",
    "enabled": false,
    "sender_env": "P2PCHASE_GMAIL_SENDER",
    "credentials_env": "P2PCHASE_GMAIL_CREDENTIALS",
    "token_env": "P2PCHASE_GMAIL_TOKEN"
  }
}
